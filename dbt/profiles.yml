hm_metrics:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: postgres
      password: "1234"
      port: 5435
      dbname: events_db
      schema: public
      threads: 1
